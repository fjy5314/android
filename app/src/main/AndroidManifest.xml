<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="c.example.test">
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MapActivity"  />
        <activity android:name=".activity.TaskActivity"  />
        <activity android:name=".activity.ReportActivity"  />
        <activity android:name=".activity.ScheduleActivity"  />
        <activity android:name=".activity.EventRecordActivity"  />
        <activity android:name=".activity.CommunicationListActivity"  />
        <activity android:name=".activity.LawActivity"  />
        <activity android:name=".activity.MessageActivity"  />
    </application>

</manifest>